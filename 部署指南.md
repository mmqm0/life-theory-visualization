# 生命本质理论可视化系统 - 部署指南

## 📋 目录

1. [系统要求](#系统要求)
2. [快速开始（推荐）](#快速开始推荐)
3. [Docker 部署](#docker-部署)
4. [Node.js 部署](#nodejs-部署)
5. [Ollama 配置](#ollama-配置)
6. [常见问题](#常见问题)

---

## 🖥️ 系统要求

### 最低配置
- **操作系统**: Windows 10/11, macOS 10.14+, Linux (Ubuntu 18.04+)
- **内存**: 4GB RAM
- **磁盘空间**: 2GB
- **Node.js**: 20.0 或更高版本

### 推荐配置
- **操作系统**: Windows 10/11, macOS 12+, Linux (Ubuntu 20.04+)
- **内存**: 8GB+ RAM
- **磁盘空间**: 5GB+
- **Node.js**: 20.x 或更高版本
- **Ollama**: 最新版本

---

## 🚀 快速开始（推荐）

### Windows 用户

1. **下载并解压项目包**
   ```
   解压到: C:\LifeTheory\
   ```

2. **双击运行**
   ```
   启动系统.bat
   ```

3. **选择启动方式**
   - 选项 1: Node.js 开发模式（推荐）
   - 选项 2: Docker 模式

4. **访问应用**
   - 前端: http://localhost:3000
   - 后端: http://localhost:8080

---

## 🐳 Docker 部署

### 前置要求

- Docker Desktop 已安装并运行
- Docker Compose 可用

### 快速启动

1. **进入项目目录**
   ```bash
   cd life-theory-visualization
   ```

2. **启动所有服务**
   ```bash
   docker-compose up -d
   ```

3. **查看日志**
   ```bash
   docker-compose logs -f
   ```

4. **停止服务**
   ```bash
   docker-compose down
   ```

### 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| 前端 | 3000 | React 应用 |
| 后端 | 8080 | Express API |
| PostgreSQL | 5432 | 数据库 |
| Neo4j | 7474 | 图数据库 Web |
| Neo4j Bolt | 7687 | 图数据库 |
| Redis | 6379 | 缓存 |

---

## 💻 Node.js 部署

### 1. 安装 Node.js

下载并安装 Node.js: https://nodejs.org/

### 2. 安装项目依赖

```bash
# 后端
cd backend
npm install

# 前端
cd ../frontend
npm install
```

### 3. 配置环境变量

**后端配置** (`backend/.env`):
```env
PORT=8080
NODE_ENV=development

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=life_theory
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Ollama (本地 AI)
AI_PROVIDER=ollama
OLLAMA_API_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3:8b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
```

**前端配置** (`frontend/.env`):
```env
REACT_APP_API_URL=http://localhost:8080/api
```

### 4. 启动数据库

**选项 A: 使用 Docker（推荐）**
```bash
docker-compose up -d postgres neo4j redis
```

**选项 B: 本地安装**
- PostgreSQL: https://www.postgresql.org/
- Neo4j: https://neo4j.com/
- Redis: https://redis.io/

### 5. 启动应用

**启动后端:**
```bash
cd backend
npm start
```

**启动前端:**
```bash
cd frontend
npm start
```

---

## 🤖 Ollama 配置

### 1. 安装 Ollama

下载地址: https://ollama.ai/

### 2. 下载模型

打开终端（PowerShell 或 Command Prompt）:

```bash
# Llama 3（推荐，8B参数）
ollama pull llama3:8b

# 嵌入模型（必需）
ollama pull nomic-embed-text

# 可选：其他模型
ollama pull qwen3:0.6b      # 轻量快速
ollama pull gemma4:latest    # Google 模型
ollama pull qwen3.5:4b      # 通义千问
```

### 3. 验证 Ollama 运行

```bash
# 检查 Ollama 是否运行
curl http://localhost:11434/api/tags

# 或在浏览器访问
http://localhost:11434/api/tags
```

### 4. 在应用中使用 Ollama

确保 `backend/.env` 中的配置正确:
```env
AI_PROVIDER=ollama
OLLAMA_API_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3:8b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
```

### 5. 在应用中切换模型

在应用界面:
1. 打开 **AI 服务设置**
2. 在 **Ollama 模型配置** 中选择模型
3. 点击保存

配置会自动保存到 `backend/ai-config.json`

---

## 🔧 常见问题

### Q: 后端启动失败，提示数据库连接错误

**A:** 请确保 PostgreSQL 和 Neo4j 正在运行:
- 检查 Docker: `docker-compose ps`
- 检查端口: 5432, 7687, 6379 是否被占用

### Q: Ollama 无法连接

**A:** 
1. 确认 Ollama 正在运行（系统托盘有图标）
2. 访问 http://localhost:11434 检查服务
3. 检查防火墙设置

### Q: 前端页面空白

**A:**
1. 检查后端是否正常启动（http://localhost:8080/api/health）
2. 检查前端 .env 中的 API 地址是否正确
3. 打开浏览器开发者工具（F12）查看错误

### Q: 模型响应很慢

**A:**
- 建议使用 qwen3:0.6b 等轻量模型
- 增加系统内存
- 关闭其他占用资源的应用

### Q: 如何完全重置应用？

**A:**
```bash
# 停止 Docker 并删除数据
docker-compose down -v

# 删除配置文件
rm backend/.env
rm backend/ai-config.json
```

### Q: 可以在多台电脑上使用吗？

**A:** 可以！您有两种方式:
1. 使用 Docker（推荐）
2. 复制整个项目文件夹（需要已安装 Node.js）

---

## 📚 更多资源

- **项目文档**: README.md
- **GitHub**: https://github.com/mmqm0/life-theory-visualization
- **Ollama**: https://ollama.ai/
- **Docker**: https://www.docker.com/

---

## 💬 技术支持

如有问题，请:
1. 查看本文档的常见问题
2. 检查 GitHub Issues
3. 提交 Issue 到仓库

---

## 📄 许可证

MIT License